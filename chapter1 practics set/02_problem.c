#include <stdio.h>

int main(){
    int r = 5; 
    int h = 16;
    printf("The area of radius %d is %f\n", r , 3.14*r*r);
    printf("The volume the cylinder of redius %d,height %d is %f", r,h,3.14*r*r*h);
    return 0;
}